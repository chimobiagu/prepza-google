import re
import os
from generate_commerce_banks import get_commerce_topic, escape_kt

# We have the 450 Commerce questions from the user prompt.
# Let's read the prompt table or load from raw text.
with open("commerce_raw_table.txt", "w") as f:
    pass # we will write the table data
